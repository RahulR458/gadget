<%- include('../includes/userLoginRegHeader.ejs') %>

  <div class="container">
    <% if(typeof message !=="undefined" ){ %>
      <p style="color: red;">
          <%= message %>
      </p>
      <% } %>
    <!-- <input type="checkbox" id="flip"> -->
     <!-- <div class="toast" role="alert" aria-live="assertive" aria-atomic="true">
      <div class="toast-header">
        <img src="..." class="rounded mr-2" alt="...">
        <strong class="mr-auto">Bootstrap</strong>
        <small>11 mins ago</small>
        <button type="button" class="ml-2 mb-1 close" data-dismiss="toast" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="toast-body">
        Hello, world! This is a toast message.
      </div>
    </div> -->
    <div class="cover">
      <div class="front">
        <img src="/user/userLogin/images/frontImg.png" alt="">
      </div>
      <div class="back">
       
        <div class="text">
          <span class="text-1">Complete miles of journey <br> with one step</span>
          <span class="text-2">Let's get started</span>
        </div>
      </div>
    </div>
    <div class="forms">
      <div class="form-content">
        <div class="login-form">
          <div class="title">Login</div>
          <!-- action="/login" method="post" -->
          <form action="/login" method="post" id="loginForm" >
            <div class="input-boxes">
              <div class="input-box">
                <i class="fas fa-envelope"></i>
                <input type="email" placeholder="Enter your email" name="email" id="loginEmail"
                  onkeyup="validateLoginEmail()">
              </div>
              <span id="loginEmail_error"></span>
              <div class="input-box">
                <i class="fas fa-lock"></i>
                <input type="password" placeholder="Enter your password" name="password" id="loginPassword"
                  onkeyup="validateLoginPassword()">
              </div>
              <span id="loginPassword_error"></span>
              <div class="text"><a href="/forgot-password">Forgot password?</a></div>
              <div class="button input-box">
                <!-- onclick="return validateLoginSubmit()" -->
                <input type="submit"  value="Submit" id="submitBtn" onclick="return validateLoginSubmit()">
                <!-- <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#exampleModal" onclick="return validateLoginSubmit()">
                  Submit
                </button> -->
              </div>
              <span id="loginSubmit_error"></span>
              <div class="text sign-up-text">Don't have an account? <label><a href="/signup">Sigup now</a></label></div>
            </div>
          </form>
        </div>

      </div>
    </div>
  </div> 
  <!-- <div class="modal fade" id="loginModal" tabindex="-1" role="dialog" aria-labelledby="loginModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="loginModalLabel">Login Failed</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-body">
          Invalid credentials. Please check your email and password.
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
        </div>
      </div>
    </div>
  </div> -->
  
  <!-- <script>

document.addEventListener("DOMContentLoaded", () => {
    const loginEmail = document.getElementById('loginEmail');
    const loginPassword = document.getElementById('loginPassword');
    const submitBtn = document.getElementById('submitBtn');
    const loginModal = new bootstrap.Modal(document.getElementById('loginModal'), {
      keyboard: false,
      backdrop: 'static'
    });
  
    submitBtn.addEventListener('click', (event) => {
      event.preventDefault(); // Prevent the form from submitting automatically
      
      console.log(loginPassword.value,loginEmail.value)
      fetch(`/userLogin/${loginEmail.value}/${loginPassword.value}`)
        .then((response) => response.json())
        .then((response) => {
          if (response.success === false) {
            console.log('Invalid credentials');
            loginModal.show(); 
          }
        })
        .catch((error) => {
          console.error('Error:', error);
        });
    });

  });
  </script> -->

<%- include('../includes/userLoginRegFooter.ejs') %>